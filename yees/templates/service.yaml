apiVersion: v1
kind: Service
metadata:
  name: {{ include "yees.fullname" . }}
  labels:
    {{- include "yees.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yees.selectorLabels" . | nindent 4 }}
